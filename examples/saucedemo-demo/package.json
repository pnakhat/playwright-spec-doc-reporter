{
  "name": "saucedemo-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui",
    "test:api": "playwright test tests/api",
    "report": "playwright test && open spec-doc-report/index.html"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
