{
  "name": "js-es6-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --env-file=.env node_modules/.bin/playwright test",
    "report": "node --env-file=.env node_modules/.bin/playwright test && open spec-doc-report/index.html"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "playwright-spec-doc-reporter": "file:../.."
  }
}
